import { createContext } from "react"

const AuthContext=createContext(
{
    // addEmail:(em
    // token:"",
    // isLogged:"",
    // addToken:(token)=>{},
    // removeItem:()=>{}
})
export default AuthContext;